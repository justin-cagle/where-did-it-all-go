# households

## Ownership

Household · User · membership · visibility modes · App Admin separation · auth plugin integration

## Visibility Modes

fully_shared · separate_with_joint_view · role_based · admin_controlled

## Auth Plugin Contract

Auth is pluggable via `pluggy`. Reference implementations: local auth (username + password + TOTP) and OIDC.

## Public Interface

_(populated as the module is built)_

## Emitted Events

_(populated as the module is built)_

## Consumed Events

_(populated as the module is built)_
