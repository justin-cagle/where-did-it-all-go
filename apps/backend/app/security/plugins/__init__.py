"""Auth provider plugins (pluggy implementations).

Shipped implementations:
  local — username + password + optional TOTP (fully functional)
  oidc  — OIDC / external IdP (stub, not yet functional)
"""
