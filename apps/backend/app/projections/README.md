# projections

## Ownership

Single deterministic projection engine consumed by budgets, debts, goals, and calendar.

Supports scenario / what-if analysis. Cache keyed to (inputs hash, as_of date).

## Public Interface

_(populated as the module is built)_

## Emitted Events

None (consumed by other modules, not a producer).

## Consumed Events

_(populated as the module is built)_
