# classification

## Ownership

Category (2-level hierarchy) · Tag · rules engine · transaction-type detector · IncomeSource registry

## Classification Pipeline Order (deterministic)

1. Type detection
2. Income source match (locks category regardless of merchant rules)
3. User rules
4. Fallback: uncategorized

## Public Interface

_(populated as the module is built)_

## Emitted Events

_(populated as the module is built)_

## Consumed Events

_(populated as the module is built)_
