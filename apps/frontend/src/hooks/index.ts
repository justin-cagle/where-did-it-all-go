/**
 * TanStack Query hooks — thin wrappers over domain modules and API clients.
 * SSE events from /api/v1/{household_id}/events trigger cache invalidations here.
 *
 * Hooks are exported from here as they are built out:
 *   export * from './use-accounts'
 *   export * from './use-budgets'
 *   export * from './use-transactions'
 *   etc.
 */

export {}
